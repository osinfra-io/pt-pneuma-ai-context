---
applyTo: "**/pt-pneuma-*/**"
---

# Pneuma Team Instructions

## GitHub Actions

After regional workspaces, subdirectory workspaces (`cert-manager`, `datadog`, `istio`, `opa-gatekeeper`, `onboarding`) deploy in dependency order.
